# Data

Run the dataset builder from the repository root:

```bash
python3 scripts/build_dataset.py --output-dir data/helpdesk
```

Generated dataset files are ignored by git so you can rebuild them freely.
